node_modules/
__pycache__/
.vercel/
.DS_Store
*.log

# Static export of the UI, regenerated by `npm run ui:build`.
ui/dist/

# Credentials. The template .env.example IS committed.
.env
.env.local
.env.*.local

# Artifacts: the canonical live run and the repeatability study are committed
# because README, docs/REPORT.md and the UI read them. Scratch runs are not.
artifacts/archive/
artifacts/run-1/
artifacts/repeat/
